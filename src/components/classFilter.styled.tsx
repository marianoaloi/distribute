import styled from "@emotion/styled";
import { FilterBar } from "./mediaTypeFilter.styled";

// Sits immediately left of the media-type filter FAB (right: 16px, 48px wide).
export const ClassFilterBar = styled(FilterBar)`
    right: 76px;
`
